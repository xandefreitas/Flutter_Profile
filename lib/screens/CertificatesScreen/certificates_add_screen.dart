import 'package:flutter/material.dart';

class CertificatesAddScreen extends StatelessWidget {
  const CertificatesAddScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Certificate'),
      ),
    );
  }
}
